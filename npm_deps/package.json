{
  "name": "npm_deps",
  "version": "0.58.0",
  "description": "this is just meant as a way to isolate the deps which the layer shared and not mix it with dev-deps we're using in the repo",
  "main": "index.js",
  "scripts": {
    "build": "yarn"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firemock": "^0.58.0",
    "firemodel": "^0.58.0",
    "universal-fire": "^0.58.0"
  },
  "volta": {
    "node": "14.16.0"
  }
}
