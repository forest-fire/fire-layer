{
  "name": "npm_deps",
  "version": "0.60.3",
  "description": "this is just meant as a way to isolate the deps which the layer shared and not mix it with dev-deps we're using in the repo",
  "main": "index.js",
  "scripts": {
    "build": "yarn"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firemock": "^0.60.3",
    "firemodel": "^0.60.3",
    "universal-fire": "^0.60.3"
  },
  "volta": {
    "node": "14.16.0"
  }
}
